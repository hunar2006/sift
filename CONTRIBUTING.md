# Contributing

## Development

```bash
pnpm i
pnpm build
pnpm test
pnpm lint
pnpm typecheck
```

Use conventional commits (`feat:`, `fix:`, `test:`, `docs:`, `chore:`). Add parser fixtures under `fixtures/diffs` and assertion tests beside the relevant package tests.
