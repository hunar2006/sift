# Sift

Review AI-generated code without drowning.

![Sift demo placeholder](docs/demo.gif)

Sift is a local-first review cockpit for large AI-generated diffs. It triages hunks, scores risk with deterministic rules, keeps review state on disk, and optionally links Claude Code provenance without sending code anywhere by default.

See `pnpm demo` for the reproducible demo fixture.
